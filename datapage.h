/*
 * datapage.h
 * -----------------------------------------------------------------------
 * Expone el HTML de la pagina "/datos" (grafica de historico) como una
 * cadena en PROGMEM, igual que webpage.h hace con la pagina principal.
 * -----------------------------------------------------------------------
 */
#pragma once
#include <Arduino.h>

extern const char DATA_HTML[] PROGMEM;
